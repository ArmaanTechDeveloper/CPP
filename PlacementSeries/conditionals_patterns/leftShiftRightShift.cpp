#include <iostream>

using namespace std;

int main(){
    // 5 ko ek baar left shift kardo in binary
    cout << (5<<1) << endl;
    // 5 ko ek baar right shift kardo in binary last wala phek do
    cout << (5>>2) << endl;
    // + ve numbers mein padding with 0 hoti hai and in -ve numbers dependant on the compiler
    return 0;
}